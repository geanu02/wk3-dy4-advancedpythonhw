from parkingGarage import Garage 

garage_1 = Garage()
garage_1.driver()

# available_tickets = [f"ticket_{a}" for a in range(0, 100)]
# available_pSpaces = [b for b in range(100, 200)]

# garage_2 = Garage(available_tickets, available_pSpaces)
# garage_2.driver()